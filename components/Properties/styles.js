import styled from '@emotion/styled';

export const Container = styled.section`
    overflow: auto;
    display: grid;
    grid-auto-flow: row;
    grid-gap: 20px;
    padding: 20px 0;
    align-content: start;
`;
